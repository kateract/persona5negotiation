import { PersonaFilterPipe } from './persona-filter.pipe';

describe('PersonaFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PersonaFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
